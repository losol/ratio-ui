---
"@eventuras/ratio-ui-next": patch
---

Ship the package's licence. The MPL-2.0 `LICENSE` file, the `license` and
`description` fields, and the SPDX headers on the sources all landed after the
1.0.0 tag was cut, so no published version has ever carried them — npm still
serves 0.2.0, which has neither a licence nor a description. This is also the
package's first release through the automated pipeline, so it is the first to
be published with provenance.
