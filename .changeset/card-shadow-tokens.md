---
"@eventuras/ratio-ui": minor
---

Make Card shadow elevation themeable via `--card-shadow-xs`/`-sm`/`-md` tokens. Defaults mirror the previous Tailwind shadows exactly (no change to the standard theme), and Card now carries a stable `.ratio-card` hook. The bureau theme uses these to give cards its hard offset shadow.
