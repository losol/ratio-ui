---
"@eventuras/ratio-ui": minor
---

Add dormant `bureau` theme tokens (palette, radii, font-family tokens) for light and dark. Activated via `data-theme="bureau"` + `data-color-scheme`; no change to existing themes. Chrome utilities and components follow in later PRs.
