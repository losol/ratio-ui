---
"@eventuras/ratio-ui": patch
---

`Label` now uses the same typography as the labels in `Select`, `NumberField`
and `FileUpload` (`text-sm font-medium text-(--text)`), so `TextField`,
`PhoneInput` and `Autocomplete` labels match the rest of the form fields.
